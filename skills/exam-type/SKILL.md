---
name: "exam-type"
description: "После выбора экзамена — уточнить тип (ЕГЭ/ОГЭ/вступительные). Блок до ответа."
---

# exam-type

## Цель
Уточнить **какой экзамен** готовит пользователь. Без типа — не продолжать.

## Триггер
- **Сразу после `purpose-select`**, если `purpose = "exam"`
- **До** `exam-subject`, `exam-topics`, `exam-self-assess`

## Логика
1. Показать варианты типа экзамена
2. Получить ответ
3. **Записать в `users/<user_key>/profile.md` → `exam_type: ege|oge|admission|other`**
4. Если «другой» — `exam_type_note: <текст>`
5. Подтвердить и перейти к `exam-subject`

## Формат сообщения

> 📝 Подготовка к **экзамену**. Какой именно?
>
> 1. **ЕГЭ**
> 2. **ОГЭ**
> 3. **Вступительные** (в вуз / колледж)
> 4. **Другой** (напишу какой)
>
> Цифра или слово — что угодно.

## Поведение

| Ответ | exam_type | Реакция |
|---|---|---|
| `1` / `егэ` / `ege` | `ege` | `Записал: ЕГЭ.` |
| `2` / `огэ` / `oge` | `oge` | `Записал: ОГЭ.` |
| `3` / `вступительные` / `вуз` / `колледж` | `admission` | `Записал: вступительные.` |
| `4` / `другой` | `other` | Спросить: «Какой экзамен?» → `exam_type_note` |

## Данные
- `users/<user_key>/profile.md` → `exam_type`, опционально `exam_type_note`

## Зависимости
- Зависит от `purpose-select` (purpose = `exam`)
- Перед `exam-subject`

## Где живёт реальное исполнение

**`SOUL.md` → секция «Старт сессии» → экзаменационная ветка (шаг 10)** — это то, что агент реально выполняет. Этот скилл — **дизайн-документ** для справки и эволюции.
